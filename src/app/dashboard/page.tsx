import { DashboardClient } from "./dashboard-client";

export const metadata = {
  title: "Dashboard | AppForge",
  description: "Manage your mobile apps built with AI",
};

export default function DashboardPage() {
  return <DashboardClient />;
}