import { SignupClient } from "./signup-client";

export const metadata = {
  title: "Sign Up | AppForge",
  description: "Create your AppForge account and start building amazing mobile apps with AI.",
};

export default function SignupPage() {
  return <SignupClient />;
}